import { Router } from 'express'; import { UserRole } from '@prisma/client'; import { protect } from '../../middlewares/auth.middleware'; import { restrictTo } from '../../middlewares/rbac.middleware'; import { AnalyticsController } from './analytics.controller'; const router = Router(); const controller = new AnalyticsController(); router.use(protect); router.get('/overview', restrictTo(UserRole.ADMIN), controller.overview); router.get('/farmer', restrictTo(UserRole.FARMER), controller.farmer); export default router;
